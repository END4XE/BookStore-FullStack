<Window x:Class="BookStoreClient.OrderManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookStoreClient"
        mc:Ignorable="d"
        Title="Управление заказами (Администратор/Менеджер)" 
        Height="400" Width="450" 
        WindowStartupLocation="CenterOwner"
        FontFamily="Calibri" FontSize="14">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header=" Поиск заказа по номеру " Grid.Row="0" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox x:Name="SearchOrderIdTextBox" Width="200" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                <Button x:Name="SearchButton" Content="Найти" Width="100" Click="SearchButton_Click"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header=" Информация о заказе " Grid.Row="2" x:Name="DetailsGroupBox" Visibility="Collapsed">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Дата заказа:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBlock x:Name="OrderDateTextBlock" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Margin="0,5,0,5"/>

                <TextBlock Text="Клиент (ФИО):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBlock x:Name="ClientFioTextBlock" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Margin="0,5,0,5"/>

                <TextBlock Text="Дата доставки:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <DatePicker x:Name="DeliveryDatePicker" Grid.Row="2" Grid.Column="1" Margin="0,5,0,5"/>

                <TextBlock Text="Статус заказа:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <ComboBox x:Name="StatusComboBox" Grid.Row="3" Grid.Column="1" Margin="0,5,0,5">
                    <ComboBoxItem Content="Новый"/>
                    <ComboBoxItem Content="В обработке"/>
                    <ComboBoxItem Content="Завершен"/>
                </ComboBox>
            </Grid>
        </GroupBox>

        <Button x:Name="SaveButton" Content="Сохранить изменения" Grid.Row="3" 
                Height="35" Width="200" HorizontalAlignment="Right" Margin="0,15,0,0"
                Visibility="Collapsed" Click="SaveButton_Click"/>
    </Grid>
</Window>
